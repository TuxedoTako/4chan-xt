{
  "addons": {
    "<%= meta.appidGecko %>": {
      "updates": [
        {
          "version": "<%= readJSON('/misc/version.json').version %>",
          "update_link": "<%= meta.downloads %><%= name %><%= channel %>.crx"
        }
      ]
    }
  }
}
